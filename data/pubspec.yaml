name: data
publish_to: 'none'

environment:
  sdk: ">=2.17.0"

dev_dependencies:
  flutter_lints: ^4.0.0

dependencies:
  domain:
    path: ../domain

  get_it: ^7.6.7

  # remote
  http: ^1.1.0
  intl: ^0.19.0

  # local
  shared_preferences: ^2.2.3
  flutter_secure_storage: ^9.2.2

  # external
  firebase_messaging: ^15.1.0
  firebase_core: ^3.4.0
  